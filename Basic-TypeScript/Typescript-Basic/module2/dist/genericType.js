"use strict";
{
    //generic type
    //type k dynamic vabe change or reuse kora
    //normally array declare
    const roll = [3, 453, 5];
    const mentors = ['j', 'sd'];
    const boolArray = [true, false, true];
    //we can write also in this way
    const roll1 = [3, 453, 5];
    const mentors1 = ['j', 'sd'];
    const boolArray1 = [true, false, true];
    const add = (x, y) => x + y;
    const roll3 = [3, 453, 5];
    const mentors3 = ['j', 'sd'];
    const boolArray3 = [true, false, true];
    //obj
    const user = [
        { name: 'JS', age: 100 },
        { name: 'S', age: 20 },
    ];
    const manush = ['x', 'y'];
    const userWIthId = [345, { name: 'js', email: 'd@' }]; //generic array tupe with number and object..and reuse upper tuple in two things
    const details = ['Jewel', 205, 405, { des: 'developer', area: 'Dhaka' }];
}
