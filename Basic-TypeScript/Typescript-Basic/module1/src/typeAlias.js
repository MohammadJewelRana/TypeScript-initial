"use strict";
{
    //two user
    //where we use upper type user
    const user = {
        id: 1,
        contact: '017',
        address: 'mirpur',
        balance: 0
    };
    const user2 = {
        id: 2,
        contact: '015',
        address: 'tangail'
    };
    const isAdmin = true; //use type
    const add = (num1, num2) => num1 + num2;
}
